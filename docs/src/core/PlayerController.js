export class PlayerController {
    constructor() {
        this.position = {
            x: 0,
            y: 5,
            z: 0
        };

        this.velocity = {
            x: 0,
            y: 0,
            z: 0
        };

        this.isFlying = false;
    }

    update(deltaTime) {
        // lógica futura
    }
}
